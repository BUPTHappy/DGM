# UCGM 贝叶斯优化参数简化总结

## 修改概述

根据你的要求，我们已经成功将UCGM的贝叶斯优化参数从16个减少到8个，只保留重叠参数进行优化，固定参数使用默认值以保证训练稳定性。

## 修改的文件

### 1. `adaptive_bayesian_optimizer.py`
- **修改内容**: 只优化8个参数，固定参数在`_build_ucgmts_config`中设置默认值
- **优化参数**: `consistc_ratio`, `rfba_gap_end`, `extrapol_ratio`, `num_sampling_steps`, `temperature`, `cfg`, `window_size`, `lambda_local`
- **固定参数**: `transport_type`, `scaled_cbl_eps`, `ema_decay_rate`, `lab_drop_ratio`, `enhanced_ratio`, `wt_cosine_loss`, `weight_function`, `time_dist_ctrl`

### 2. `training_bayesian_optimizer.py`
- **修改内容**: 更新所有参数应用函数，只使用8个优化参数
- **主要函数更新**:
  - `_create_temp_config()`: 只设置优化参数，固定参数使用默认值
  - `evaluate_model_with_params()`: 只更新优化参数
  - `apply_best_params_to_model()`: 只应用优化参数到模型
  - 所有验证和打印函数更新

## 优化参数（8个）

### UCGM重叠参数（3个）
1. **`consistc_ratio`**: 一致性比率
   - 训练作用: 控制训练策略（多步 vs 少步）
   - 采样作用: 控制随机性比率
   - 优化范围: `[0.5, 1.0]` (Speed/Balanced), `[0.0, 1.0]` (Performance)

2. **`rfba_gap_end`**: 边界间隙结束点
   - 训练作用: 时间步调度控制
   - 采样作用: 采样时间步边界控制
   - 优化范围: `[0.1, 0.8]` (Speed/Balanced), `[0.001, 0.8]` (Performance)

3. **`extrapol_ratio`**: 外推比率
   - 训练作用: 采样加速
   - 采样作用: 采样加速
   - 优化范围: `[0.0, 0.6]`

### 采样独有参数（5个）
4. **`num_sampling_steps`**: 采样步数
   - 优化范围: `[1, 2]` (Speed), `[2, 3, 4, 5, 6, 7, 8, 9, 10]` (Performance), `[1, 2, 3]` (Balanced)

5. **`temperature`**: 温度参数
   - 优化范围: `[0.7, 1.2]`

6. **`cfg`**: 分类器自由引导
   - 优化范围: `[0.8, 1.5]` (Speed/Balanced), `[1.0, 2.0]` (Performance)

7. **`window_size`**: 局部注意力窗口大小
   - 优化范围: `[0, 15]`

8. **`lambda_local`**: 局部特征融合参数
   - 优化范围: `[0.01, 0.8]`

## 固定参数（8个）

这些参数使用模型默认值，不进行显式设置，保证训练稳定性：

1. **`transport_type`** = "Linear" (默认)
2. **`scaled_cbl_eps`** = 0.0 (默认)
3. **`ema_decay_rate`** = 0.999 (默认)
4. **`lab_drop_ratio`** = 0.1 (默认)
5. **`enhanced_ratio`** = 0.0 (默认)
6. **`wt_cosine_loss`** = False (默认)
7. **`weight_function`** = None (默认)
8. **`time_dist_ctrl`** = [1.0, 1.0, 1.0] (默认)

## 优势

### 1. 训练稳定性
- 固定参数使用模型默认值，避免训练不稳定
- 只优化重叠参数，减少参数冲突

### 2. 优化效率
- 参数空间从16维减少到8维
- 贝叶斯优化收敛更快
- 减少计算资源消耗

### 3. 风险控制
- 避免训练独有参数的不当优化
- 保持训练过程的稳定性
- 降低优化失败的风险

### 4. 参数一致性
- 训练和采样参数协同优化
- 使用同一个目标函数评估
- 避免参数不匹配问题

### 5. 代码简洁性
- 不需要手动设置固定参数
- 使用模型默认值，减少代码复杂度
- 降低维护成本

## 使用建议

1. **推荐模式**: 使用Balanced模式进行优化
2. **监控指标**: 重点关注训练损失和采样性能
3. **参数调整**: 如果发现训练不稳定，可以进一步缩小重叠参数的优化范围
4. **验证方法**: 使用同一个目标函数评估训练和采样参数组合

## 预期效果

- **训练稳定性**: 通过固定训练独有参数，保证训练过程稳定
- **优化效率**: 只优化关键重叠参数，提高优化效率
- **性能提升**: 通过优化重叠参数，同时改善训练和采样性能
- **风险控制**: 避免训练独有参数的不当优化导致训练失败

这种方案既保证了训练稳定性，又能通过优化重叠参数来提升整体性能，是一个平衡的优化策略。
